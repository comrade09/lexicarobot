# --- Telegram -----------------------------------------------------------
# Kurigram replaces the `pyrogram` package namespace, so `from pyrogram
# import ...` throughout this codebase keeps working unchanged. It's the
# fork that actually implements colored inline buttons (style="primary"/
# "danger"/"success") — pyrofork does not support this, which is why you
# hit `InlineKeyboardButton.__init__() got an unexpected keyword argument
# 'style'`. Do NOT also list plain `pyrogram` or `pyrofork` here — they
# all install under the same `pyrogram` package name, so having more than
# one is a coin-flip on which you actually get.
kurigram
TgCrypto
# --- Web server (streaming routes) --------------------------------------
aiohttp
# --- Database -------------------------------------------------------------
pymongo
dnspython
# --- Misc, actually imported somewhere in this codebase --------------------
pytz
humanize
openai          # plugins/custom_filters.py
